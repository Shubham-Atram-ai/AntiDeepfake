# Environment variables for AntiDeepfake frontend
# This file is read by Vite at build time (VITE_ prefix required for client exposure)

# Backend API base URL – change to your deployed URL in production
VITE_API_URL=http://localhost:8000
